# 后端开发日志 - 2026/04/14

## 今日完成

### 1. 项目初始化
- 初始化 Git 仓库，连接 GitHub 远程仓库
- 创建后端目录结构（cmd、internal、migrations 等）
- 配置 .gitignore（排除编译产物、敏感文件、IDE 配置等）

### 2. Go 项目搭建
- `go mod init` 初始化 Go 模块
- 安装核心依赖：Gin、MySQL 驱动、Redis 客户端、MinIO SDK、JWT、Viper、crypto
- 编写最小可运行的 `main.go`，Gin 服务器成功启动并响应 `/ping`

### 3. 本地开发环境（Docker Compose）
- 编写 `docker-compose.yml`，一键启动 MySQL 8.0、Redis 7、MinIO
- 三个服务均启动成功，状态为 healthy
- MinIO 管理界面可通过 `http://localhost:9001` 访问

### 4. 配置管理
- 编写 `config.yaml` 统一管理所有服务配置（端口、密码、超时等）
- 编写 `config.go` 使用 Viper 读取 YAML 配置并映射到 Go 结构体
- Go 程序成功读取配置并连接 MySQL 和 Redis

### 5. 数据库设计
- 编写迁移 SQL `001_init_tables.sql`，创建 5 张表：
  - `users` — 用户账号信息、存储配额
  - `folders` — 文件夹树结构（物化路径）
  - `files` — 文件元信息、MD5 哈希（秒传核心）
  - `upload_sessions` — 大文件上传任务
  - `upload_chunks` — 分片上传记录
- 手动执行 SQL，5 张表建表成功


## 明日计划

- Week 2：用户认证系统
  - 编写 User 模型和 Repository
  - 实现 JWT 工具包和密码哈希
  - 实现注册、登录接口
  - 编写认证中间件
